# FastAPI Core
fastapi
uvicorn[standard]
python-multipart

# Async HTTP client
httpx

# WebSocket support
python-socketio

# Data validation
pydantic

# File handling
aiofiles

# Environment and configuration
python-dotenv

# CORS middleware (included in FastAPI but explicit for clarity)
starlette

# Session middleware
itsdangerous

# Audio processing (assuming these are your existing modules)
beautifulsoup4
soupsieve

# Speech recognition (add these if needed for your modules)
SpeechRecognition
pyaudio
pydub

# Google Generative AI - Version spécifique pour éviter les bugs
google-generativeai>=0.8.0

# Additional dependencies that might be needed
requests
numpy

# Google GenAI SDK (pour TTS)
google-genai>=0.1.0
python-dotenv